---
type: llm
weight: 1
---

A successful response answers in terms of js_of_ocaml and Lwt_js_events (a single
`keydowns` loop or a key-state table, `Lwt_js.sleep` or `requestAnimationFrame` for the
frame loop) and does not introduce Eliom concepts such as `let%client`, `Eliom.Content`,
services or RPCs, which do not apply to a browser-only program.
